{
  "name": "muchat-proxy-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "cloudflare": {
    "bindings": {
      "API_KEYS": {
        "description": "调用本代理时使用的 Key"
      },
      "MUC_USERNAME": {
        "description": "学号"
      },
      "MUC_PASSWORD": {
        "description": "信息门户密码"
      }
    }
  },
  "dependencies": {
    "sm-crypto": "^0.4.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.3",
    "@cloudflare/workers-types": "^4.20260510.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.90.0"
  }
}
